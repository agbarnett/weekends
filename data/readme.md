# Data explanation

The two files are for reviewers (`.RData`) and reviews (`.RData`).

The variables are:
* local.date
* local.hour
* window
* country
* journal
* timezoneId
* weekend
* latenight
* holiday
